name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

# Opt into Node.js 24 for JavaScript actions (Node 20 is deprecated; default
# flips 2026-06-02). Becomes a no-op once Node 24 is the runner default.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11', '3.12', '3.13']
        django-version: ['4.2', '5.0', '5.2']
        # Django 4.2 supports 3.10-3.12; 5.0 supports 3.10-3.12; 5.2 supports 3.10-3.13.
        # Python 3.13 support landed in Django 5.1 — exclude older Django lines.
        exclude:
          - python-version: '3.13'
            django-version: '4.2'
          - python-version: '3.13'
            django-version: '5.0'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
          cache-dependency-path: requirements-dev.txt

      - name: Install pinned Django
        run: |
          python -m pip install --upgrade pip
          pip install "Django~=${{ matrix.django-version }}.0"

      - name: Install dev dependencies
        run: pip install -r requirements-dev.txt

      - name: Install package (editable)
        run: pip install -e .

      - name: Import check
        run: |
          python - <<'PY'
          import django_mcp
          from django_mcp.apps import DjangoMcpConfig
          print("django-mcp-framework", django_mcp.__version__)
          print("AppConfig:", DjangoMcpConfig.name)
          PY

      - name: Lint (ruff check)
        run: ruff check django_mcp/ tests/

      - name: Format check (ruff format)
        run: ruff format --check django_mcp/ tests/

      - name: Test suite (pytest)
        run: pytest -q

  build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install build tooling
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build sdist/wheel
        run: python -m build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7
